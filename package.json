{
  "name": "prisma-bug",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "docker run --name basic-postgres --rm -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=pass -p 5430:5432 -it  -v ./init.sql:/docker-entrypoint-initdb.d/init.sql postgres:13",
    "test": "jest"
  },
  "description": "",
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "@types/node": "^20.14.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "prisma": "^5.15.0",
    "ts-jest": "^29.1.4"
  }
}
